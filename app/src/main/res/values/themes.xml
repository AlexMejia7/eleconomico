<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Tema base de la app -->
    <style name="Base.Theme.Eleconomico" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Puedes personalizar colores aquí -->
    </style>

    <!-- Tema normal de la app -->
    <style name="Theme.Eleconomico" parent="Base.Theme.Eleconomico" />

    <!-- Tema especial para SplashActivity -->
    <style name="Theme.Eleconomico.Splash" parent="Theme.Material3.DayNight.NoActionBar">
        <item name="android:windowBackground">@color/white</item> <!-- Fondo blanco -->
        <item name="android:windowNoTitle">true</item> <!-- Sin título -->
        <item name="android:windowFullscreen">true</item> <!-- Pantalla completa -->
        <item name="android:statusBarColor">@android:color/transparent</item> <!-- Barra transparente -->
    </style>

</resources>
